# N07 Admin / P10 Memory

Entity memory, procedural memory, and the knowledge this nucleus carries across sessions.

## Example kinds (in P10, this checkout)
- `prospective_memory` -- Scheduled future actions and reminders that an agent must execute at a future time or trigger condition
- `workflow_run_crate` -- RO-Crate 1.2 Workflow Run Crate: scientific workflow execution provenance with CreateAction, ORCID attribut...
- `agent_grounding_record` -- Per-inference provenance record: tool calls, RAG chunks, model signature, output hash, OTel/C2PA traceability

## Schema
See [N00_genesis/P10_memory/_schema.yaml](../../N00_genesis/P10_memory/_schema.yaml) for this pillar's field contract.

---
This pillar is empty by design -- it fills the first time one of your builds writes here. See [HOME -> Anatomy](../../HOME.md#anatomy-why-nuclei-look-incomplete).
