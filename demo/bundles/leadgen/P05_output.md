---
kind: output_template
id: bld_output_template_research_pipeline
pillar: P05
llm_function: PRODUCE
purpose: Template com {{vars}} para a produção da config de research pipeline
quality: null
title: "Modelo de Saída: Pipeline de Pesquisa"
version: "1.0.0"
author: n03_builder
tags:
  - "research_pipeline"
  - "builder"
  - "examples"
tldr: "Exemplos-modelo e anti-exemplos para a construção de pipelines de pesquisa, demonstrando a estrutura ideal e as armadilhas mais comuns."
domain: "construção de pipeline de pesquisa"
created: "2026-04-07"
updated: "2026-04-07"
8f: "F6_produce"
keywords:
  - "template with"
  - "research pipeline construction"
  - "output template research pipeline"
  - "research_pipeline"
  - "builder"
  - "examples"
  - "## artifact file template"
  - "output template"
  - "config file template"
  - "research pipeline config"
density_score: 0.90
related:
  - research-pipeline-builder
---
# Modelo de Saída: research_pipeline

## Modelo de Arquivo de Config
```yaml
# Research Pipeline Config — {{empresa}}
# Generated by research-pipeline-builder v1.0.0

identity:
  empresa: "{{empresa}}"
  nicho: "{{nicho}}"
  idioma: "{{idioma}}"        # pt-BR, en, es
  pais: "{{pais}}"            # BR, US, EU

sources:
  inbound:                     # marketplace / product catalog sources
    {{#each inbound_sources}}
    - {{this}}
    {{/each}}
  outbound:                    # social / review / community sources
    {{#each outbound_sources}}
    - {{this}}
    {{/each}}
  search:                      # web search engines
    {{#each search_sources}}
    - {{this}}
    {{/each}}
  trends:                      # trend / price tracking
    {{#each trend_sources}}
    - {{this}}
    {{/each}}
  rag:                         # internal knowledge base
    {{#each rag_sources}}
    - {{this}}
    {{/each}}

storm_perspectives:
  {{#each perspectives}}
  - role: "{{this.role}}"
    focus: "{{this.focus}}"
  {{/each}}

multi_model:
  extraction: "{{model_extraction}}"   # fast model for structured data
  reasoning: "{{model_reasoning}}"     # strong model for analysis
  social: "{{model_social}}"           # fast model for volume data
  critic: "{{model_critic}}"           # thinking model for verification

budget:
  firecrawl_monthly: {{firecrawl_monthly}}
  firecrawl_per_research: {{firecrawl_per_research}}
  serper_daily: {{serper_daily}}

output:
  formats: [{{output_formats}}]       # html, pptx, json
  idioma: "{{output_idioma}}"
  template: "{{output_template}}"     # consulting, academic, brief

quality:
  crag_min_score: {{crag_min}}        # 0.0-1.0, default 0.7
  critic_max_iterations: {{critic_max}} # 1-5, default 3
  final_min_score: {{final_min}}       # 1.0-10.0, default 8.0

marketplace_schemas:                   # extraction prompts per marketplace
  {{#each marketplace_schemas}}
  {{@key}}:
    fields: [{{this.fields}}]
  {{/each}}
```

## Modelo de Arquivo de Artefato
```markdown
---
id: {{artifact_id}}
kind: cli_tool
pillar: P04
title: "{{title}}"
version: "1.0.0"
created: "{{date}}"
author: "research-pipeline-builder"
domain: research_pipeline
quality: null
tags: [research-pipeline, STORM, CRAG, {{nicho}}]
---
# {{title}}
## Pipeline (7 stages)
## Sources
## Config Reference
## Quality Gates
```

## Padrões de Template

1. Defina todas as seções obrigatórias para este kind de output
2. Inclua o schema de frontmatter com os campos obrigatórios
3. Forneça marcadores estruturais para a pós-validação
4. Especifique restrições de formato para markdown, YAML e JSON
5. Referencie o validation_schema para verificações automatizadas

## Properties

| Property | Value |
|----------|-------|
| Kind | `output_template` |
| Pillar | P05 |
| Domain | research pipeline construction |
| Pipeline | 8F (F1-F8) |
| Scorer | cex_score.py |
| Compiler | cex_compile.py |
| Retriever | cex_retriever.py |
| Quality target | 9.0+ |
| Density target | 0.85+ |

## Related Artifacts
| Artifact | Relationship | Score |
|----------|-------------|-------|
| tpl_research_pipeline | upstream | 0.34 |
| p02_agent_research_pipeline_intelligence | upstream | 0.30 |
| [[research-pipeline-builder]] | upstream | 0.27 |
| [[bld_prompt_research_pipeline]] | upstream | 0.26 |
