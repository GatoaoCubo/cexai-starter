# [fornecer: nome da marca (brand_config.identity.BRAND_NAME)] -- Media and Photo -- exported agent package

Portable, single-capability agent exported from CEXAI. Capability `media_photo` (nucleus N02 . kind `multimodal_prompt` . pillar P03), tenant `public`, target `customgpt`.

## Contents
- `manifest.yaml` -- the CEX agent_package manifest (kind=agent_package).
- `system_instruction.md` -- the target-agnostic system prompt.
- `agent_card.json` -- an A2A AgentCard (one skill = this capability).
- `customgpt_instructions.json` -- the customgpt rendering of this agent.
- `README.md` -- this file.

## Import (customgpt)
GPT Builder (ChatGPT): create a new GPT, open Configure, paste the contents of customgpt_instructions.json's "instructions" into the Instructions box, and add the conversation_starters.

## Provenance / honesty
Never-fabricate: any `[fornecer: ...]` marker is a field with no real input -- fill it in before use. Notes:
- brand_config.yaml not found for tenant 'public' -- brand fields are placeholders (never fabricated).
- no typed input_contract declared for this capability -- the agent takes a single free-text 'intent' input.

_Generated by `_tools/cex_export_agent.py` on 2026-07-17T07:08:24+00:00. Offline, read-only, no DB/prod._
