// brand.config.ts -- tenant brand overlay (cex_distill).
// Brand tokens were not supplied by the source -> honest placeholder (never-fabricate).
// Populate via cex_moldgen_emit from brand/brand_config.yaml.
export const brand = {
  name: "Sua Empresa",
  slug: "starter",
};
